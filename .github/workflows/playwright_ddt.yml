name: Playwright DDT Login Tests
on:
  push:
    branches: [ main ]
  workflow_dispatch: 

jobs:
  test:
    timeout-minutes: 15
    runs-on: ubuntu-latest
    env:
      BASE_URL: ${{ vars.URL_PRODUCCION }} 

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Instalar dependencias
        run: npm install

      # ✅ SOLUCIÓN MAESTRA: Instalamos usando el comando global
      - name: Install Playwright Browsers
        run: npx playwright install chromium --with-deps

      - name: Ejecutar pruebas de Login DDT
        # Agregamos 'sh' para asegurar que el intérprete de comandos tenga el control
        run: npx playwright test --reporter=html

      - name: Subir Reporte HTML
        uses: actions/upload-artifact@v4
        if: always() 
        with:
          name: reporte-playwright
          path: playwright-report/
          retention-days: 7
